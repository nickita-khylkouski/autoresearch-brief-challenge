# Tasks

This public repo only ships the visible `dev` split.

Official held-out splits are not published here:

- `public_leaderboard`: 20 tasks
- `private_leaderboard`: 40 tasks

Maintainers evaluate those separately to keep the tournament honest.
